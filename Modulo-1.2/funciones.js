// Crear una función que reciba un número y devuelva el doble

function doble2 (a) {
    return a * 2
}

const mult = doble2 (4) // si añadimos la variable para que aparezca en consola, ya que detrás del return termina la función
console.log(mult)

function doble(num) { // la muestra en consola
    console.log (num * 2)
    
}
doble(3)

