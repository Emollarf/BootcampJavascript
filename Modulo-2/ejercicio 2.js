//  Crea una función que reciba un número y devuelva el cuadrado de ese número.

function cuadrado2 (a) {
    return a*a  
}

const multA = cuadrado2 (4) // añadimos para que aparezca en consola
console.log(multA)



function cuadrado (a) { // aparece en consola
    console.log (a * a)
}
cuadrado(4)